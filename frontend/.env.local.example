# ShareHub Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# Backend API URL
# Development: http://localhost:3001
# Production: Your deployed backend URL (Railway/Render/Fly.io)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Supabase Configuration
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://agmkgrpxgpzqscbkizdl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (Server-side only - for cleanup scheduled function)
# WARNING: This key bypasses RLS. Keep it secret! Never expose to client.
# Get from: https://app.supabase.com/project/_/settings/api
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Cloudflare R2 Storage Configuration (Server-side only - NOT NEXT_PUBLIC)
# Get these from: Cloudflare Dashboard → R2 → Manage R2 API Tokens
# IMPORTANT: These are server-side secrets, never expose to client
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=sharehub-slides

# Note: In production (Netlify), set these variables in:
# Site settings → Environment variables → Add a variable
# Make sure R2 variables are NOT marked as public (no NEXT_PUBLIC prefix)
